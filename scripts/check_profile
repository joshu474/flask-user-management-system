import sqlite3

conn = sqlite3.connect("users.db")
cursor = conn.cursor()

cursor.execute("PRAGMA table_info(users)")
print("Columns:")
for row in cursor.fetchall():
    print(row)

cursor.execute("SELECT * FROM users WHERE username=?", ("Joshua Dan",))
print("\nJoshua Dan:")
print(cursor.fetchone())

conn.close()
